const numeros = [2, 3, 45, 3, 12, , 7, 8];
const masNumeros = [3, 4, 7, 8, 9, 0, ...numeros];
if (masNumeros.includes(12)) {
    console.log("SI se encuentra xd")
} else "NO se encuentra"


// elevar a lap potencia
let x = 2 // expnetes
let y = 10 // base
let potencia = y ** x
console.log(potencia)